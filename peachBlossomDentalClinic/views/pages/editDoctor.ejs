<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Tailwind CSS -->
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>

<body class="min-h-screen bg-gradient-to-br from-[#FF9C87] via-white to-[#66B3FF] flex items-center justify-center py-2">

  <!-- Form Container -->
  <div class="w-full max-w-3xl bg-white/70 backdrop-blur-xl border border-white/40 rounded-3xl shadow-2xl p-6 sm:p-10">

    <!-- Header -->
    <div class="flex items-center justify-center mb-1 gap-10">
      <!-- Logo -->
      <img src="/image/dentalLogo.png"
           class="h-14 w-14 border-2 border-[#FF9C87] rounded-full object-contain shadow-md"
           alt="Clinic Logo">

      <!-- Title -->
      <h1 class="text-3xl font-extrabold text-gray-800 flex items-center">
        <i class="fa-solid fa-user-doctor text-[#FF9C87] mr-2"></i>
        <%= title %>
      </h1>
    </div>

    <!-- Edit Form -->
    <form action="/admin/doctor/<%= doctor._id %>/edit" method="POST" class="space-y-5" novalidate>
<div class="grid grid-cols-2 gap-8">
      <!-- Full Name -->
      <div>
        <label class="block text-gray-700 font-semibold mb-1">Full Name <span class="text-red-500">*</span></label>
        <input type="text" name="name" value="<%= doctor.name %>" required
          class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-[#FF9C87] bg-white/80" />
         <p id="nameError" class="text-red-600 text-xs mt-1"></p>
        </div>

      <!-- Phone -->
      <div>
        <label class="block text-gray-700 font-semibold mb-1">Phone Number <span class="text-red-500">*</span></label>
        <input type="text" name="phoneNumber" value="<%= doctor.phoneNumber %>" required
          class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-[#FF9C87] bg-white/80" />
        <p id="phoneError" class="text-red-600 text-xs mt-1"></p>
       </div>

      <!-- Email -->
      <div>
        <label class="block text-gray-700 font-semibold mb-1">Email <span class="text-red-500">*</span></label>
        <input type="email" name="email" value="<%= doctor.email %>"
          class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-[#FF9C87] bg-white/80" />
          <p id="emailError" class="text-red-600 text-xs mt-1"></p>
        </div>

      <!-- Specialization -->
      <div>
        <label class="block text-gray-700 font-semibold mb-1">Specialization <span class="text-red-500">*</span></label>

        <select name="specialization"
          class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-[#FF9C87] bg-white/80">
          <option value="" hidden>Select Specialization</option>
          <option value="Orthodontics" <%= doctor.specialization === 'Orthodontics' ? 'selected' : '' %>>Orthodontics</option>
          <option value="Periodontics" <%= doctor.specialization === 'Periodontics' ? 'selected' : '' %>>Periodontics</option>
          <option value="Endodontics" <%= doctor.specialization === 'Endodontics' ? 'selected' : '' %>>Endodontics</option>
          <option value="Prosthodontics" <%= doctor.specialization === 'Prosthodontics' ? 'selected' : '' %>>Prosthodontics</option>
          <option value="Pediatric Dentistry" <%= doctor.specialization === 'Pediatric Dentistry' ? 'selected' : '' %>>Pedodontics</option>
          <option value="Maxillo-Facial Surgery" <%= doctor.specialization === 'Maxillo-Facial Surgery' ? 'selected' : '' %>>Maxillo-Facial Surgery</option>
          <option value="Cosmetic Dentistry" <%= doctor.specialization === 'Cosmetic Dentistry' ? 'selected' : '' %>>Cosmetic Dentistry</option>
          <option value="Dental Surgeon" <%= doctor.specialization === 'Dental Surgeon' ? 'selected' : '' %>>Dental Surgeon</option>
          <option value="Dental Therapist" <%= doctor.specialization === 'Dental Therapist' ? 'selected' : '' %>>Dental Therapist</option>
          <option value="Others" <%= doctor.specialization === 'Others' ? 'selected' : '' %>>Others</option>
        </select>
        <p id="specError" class="text-red-600 text-xs mt-1"></p>
      </div>

      <!-- Experience -->
      <div>
        <label class="block text-gray-700 font-semibold mb-1">Experience (Years)</label>
        <input type="number" name="experience" value="<%= doctor.experience %>"
          class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-[#FF9C87] bg-white/80" />
        <p id="expError" class="text-red-600 text-xs mt-1"></p>
      </div>

      <!-- Salary -->
      <div>
        <label class="block text-gray-700 font-semibold mb-1">Salary</label>
        <select name="salary"
          class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:outline-none focus:ring-2 focus:ring-[#FF9C87] bg-white/80">
          <option value="" hidden>Select Salary (in pounds)</option>
          <option value="10000" <%= doctor.salary == 10000 ? 'selected' : '' %>>£10000</option>
          <option value="9000" <%= doctor.salary == 9000 ? 'selected' : '' %>>£9000</option>
          <option value="8000" <%= doctor.salary == 8000 ? 'selected' : '' %>>£8000</option>
          <option value="7000" <%= doctor.salary == 7000 ? 'selected' : '' %>>£7000</option>
          <option value="6000" <%= doctor.salary == 6000 ? 'selected' : '' %>>£6000</option>
          <option value="5000" <%= doctor.salary == 5000 ? 'selected' : '' %>>£5000</option>
          <option value="4000" <%= doctor.salary == 4000 ? 'selected' : '' %>>£4000</option>
          <option value="3000" <%= doctor.salary == 3000 ? 'selected' : '' %>>£3000</option>
          <option value="2000" <%= doctor.salary == 2000 ? 'selected' : '' %>>£2000</option>
          <option value="Others" <%= doctor.salary === 'Others' ? 'selected' : '' %>>Others</option>
        </select>
       <p id="salaryError" class="text-red-600 text-xs mt-1"></p>
      </div>

      <!-- Available Days -->
      <div>
        <label class=" text-gray-700 font-semibold mb-1">Available Days</label>
        <div class="flex gap-4">  
          <% ['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'].forEach(day => { %>
            <label class="flex items-center gap-2">  
              <input type="checkbox" name="availableDays" value="<%= day %>"
                <%= doctor.availableDays.includes(day) ? 'checked' : '' %>>
              <span><%= day %></span>
            </label>
          <% }) %>
        </div>
        <p id="daysError" class="text-red-600 text-xs mt-1"></p>
      </div>
</div>


      <!-- Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center mt-6">
        <a href="/admin/doctorDetails"
           class="flex items-center justify-center gap-2 bg-[#66B3FF] text-gray-900 px-6 py-2 rounded-xl hover:bg-[#4FA0E0] transition font-semibold w-full sm:w-auto">
          <i class="fa-solid fa-arrow-left"></i> Cancel
        </a>

        <button type="submit"
           class="flex items-center justify-center gap-2 bg-[#FF9C87] text-white px-6 py-2 rounded-xl hover:bg-[#e68274] transition font-semibold w-full sm:w-auto">
          <i class="fa-solid fa-pen"></i> Update Dentist
        </button>
      </div>

    </form>

  </div>

    <!-- VALIDATION -->
  <script>
    const form = document.getElementById("formSubmit");

    const nameInput = document.getElementById("nameInput");
    const phoneInput = document.getElementById("phoneInput");
    const emailInput = document.getElementById("emailInput");
    const specInput = document.getElementById("specInput");
    const expInput = document.getElementById("expInput");
    const salaryInput = document.getElementById("salaryInput");

    const nameError = document.getElementById("nameError");
    const phoneError = document.getElementById("phoneError");
    const emailError = document.getElementById("emailError");
    const specError = document.getElementById("specError");
    const expError = document.getElementById("expError");
    const salaryError = document.getElementById("salaryError");
    const daysError = document.getElementById("daysError");

    const namePattern = /^[A-Za-z\s]+$/;
    const phonePattern = /^\d{10}$/;
    const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

    // NAME 
    function validateName() {
      if (!nameInput.value.trim()) {
        nameError.textContent = "Name is required";
        return false;
      }
      if (!namePattern.test(nameInput.value)) {
        nameError.textContent = "Only alphabets allowed";
        return false;
      }
      nameError.textContent = "";
      return true;
    }

    // PHONE
    function validatePhone() {
      if (!phoneInput.value.trim()) {
        phoneError.textContent = "Phone is required";
        return false;
      }
      if (!phonePattern.test(phoneInput.value)) {
        phoneError.textContent = "Must be 10 digits";
        return false;
      }
      phoneError.textContent = "";
      return true;
    }

    // EMAIL 
    function validateEmail() {
      if (!emailInput.value.trim()) {
        emailError.textContent = "";
        return true; 
      }
      if (!emailPattern.test(emailInput.value)) {
        emailError.textContent = "Invalid email";
        return false;
      }
      emailError.textContent = "";
      return true;
    }

    // SPECIALIZATION 
    function validateSpec() {
      if (!specInput.value) {
        specError.textContent = "Select specialization";
        return false;
      }
      specError.textContent = "";
      return true;
    }

    // EXPERIENCE 
    function validateExp() {
      if (expInput.value && expInput.value < 0) {
        expError.textContent = "Invalid experience";
        return false;
      }
      expError.textContent = "";
      return true;
    }

    // SALARY 
    function validateSalary() {
      salaryError.textContent = "";
      return true;
    }

    // DAYS 
    function validateDays() {
      const checked = document.querySelectorAll("input[name='availableDays']:checked");
      if (checked.length === 0) {
        daysError.textContent = "Select at least one day";
        return false;
      }
      daysError.textContent = "";
      return true;
    }

    //  SUBMIT
    form.addEventListener("submit", function(e) {
      const ok =
        validateName() &&
        validatePhone() &&
        validateEmail() &&
        validateSpec() &&
        validateExp() &&
        validateSalary() &&
        validateDays();

      if (!ok) e.preventDefault();
    });

    // LIVE 
    nameInput.addEventListener("blur", validateName);
    phoneInput.addEventListener("input", validatePhone);
    emailInput.addEventListener("blur", validateEmail);
    specInput.addEventListener("change", validateSpec);
    expInput.addEventListener("input", validateExp);
    salaryInput.addEventListener("change", validateSalary);
  </script>


</body>
</html>